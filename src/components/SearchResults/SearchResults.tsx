import { UserCard } from "../UserCard/UserCard";

import "./style.css";

export function SearchResults() {
  return (
    <div className="usersList">
      {/* {users.map((user) => (
        <UserCard {...user} />
      ))} */}
    </div>
  );
}
